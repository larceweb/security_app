<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:top="-30dp" android:left="-40dp" android:right="0dp" android:bottom="-190dp">
        <rotate
            android:fromDegrees="45"
            android:pivotX="10%"
            android:pivotY="50%">

            <shape android:shape="rectangle">
                <solid android:color="@color/white" /> <!-- Reemplaza con tu color deseado -->
                <corners android:radius="10dp" />
            </shape>
        </rotate>
    </item>
</layer-list>
